# Local Development Environment Variables
# Use this when running Go app locally but connecting to Docker PostgreSQL

# Environment setting
APP_ENV=development

# Database Configuration (localhost for local development)
# When running locally, PostgreSQL in Docker is accessible via localhost:5432
DB_HOST=localhost
DB_USER=restaurant
DB_PASSWORD=restaurant
DB_NAME=restaurant
DB_PORT=5432
DB_SSLMODE=disable

# Connection Pool Settings (optional)
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=10
DB_CONN_MAX_LIFETIME=1h
DB_CONN_MAX_IDLE_TIME=10m

# Alternative: Use DATABASE_URL instead of individual DB_* variables
# DATABASE_URL=postgres://restaurant:restaurant@localhost:5432/restaurant?sslmode=disable

# JWT Configuration
JWT_SECRET=kIOopC3C7wA8DQH6FOF2Jfn+UZP8Q02nGxr/EgFMOmo=

# Anthropic API
ANTHROPIC_API_KEY=dummy_key_for_dev

# Server Configuration
SERVER_ADDRESS=localhost
SERVER_PORT=8888
GRPC_ADDRESS=localhost
GRPC_PORT=50051

# Email Configuration (optional - using mock service)
EMAIL_FROM_ADDRESS=noreply@english-ai.dev
SMTP_HOST=localhost
SMTP_PORT=1025

# QuanAn Service Address (if applicable - for local development)
QUAN_AN_ADDRESS=localhost:8081

# Logging
LOG_LEVEL=debug