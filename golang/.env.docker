# Docker Environment Variables
# Use this when running Go app inside Docker container

# Environment setting
APP_ENV=docker

# Database Configuration (using Docker service names)
DB_HOST=mypostgres_ai
DB_USER=restaurant
DB_PASSWORD=restaurant
DB_NAME=restaurant
DB_PORT=5432
DB_SSLMODE=disable

# Connection Pool Settings
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=10
DB_CONN_MAX_LIFETIME=1h
DB_CONN_MAX_IDLE_TIME=10m

# JWT Configuration
JWT_SECRET=kIOopC3C7wA8DQH6FOF2Jfn+UZP8Q02nGxr/EgFMOmo=

# Anthropic API
ANTHROPIC_API_KEY=dummy_key_for_dev

# Server Configuration (bind to all interfaces in Docker)
SERVER_ADDRESS=0.0.0.0
SERVER_PORT=8888
GRPC_ADDRESS=0.0.0.0
GRPC_PORT=50051

# Email Configuration
EMAIL_FROM_ADDRESS=noreply@english-ai.dev
SMTP_HOST=localhost
SMTP_PORT=1025

# QuanAn Service Address (using Docker service name)
QUAN_AN_ADDRESS=quan_an:8081

# Logging
LOG_LEVEL=info