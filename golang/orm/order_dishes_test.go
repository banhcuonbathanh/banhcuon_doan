// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package orm

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/aarondl/randomize"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testOrderDishes(t *testing.T) {
	t.Parallel()

	query := OrderDishes()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testOrderDishesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OrderDish{}
	if err = randomize.Struct(seed, o, orderDishDBTypes, true, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := OrderDishes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testOrderDishesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OrderDish{}
	if err = randomize.Struct(seed, o, orderDishDBTypes, true, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := OrderDishes().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := OrderDishes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testOrderDishesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OrderDish{}
	if err = randomize.Struct(seed, o, orderDishDBTypes, true, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := OrderDishSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := OrderDishes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testOrderDishesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OrderDish{}
	if err = randomize.Struct(seed, o, orderDishDBTypes, true, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := OrderDishExists(ctx, tx, o.OrderID, o.DishID)
	if err != nil {
		t.Errorf("Unable to check if OrderDish exists: %s", err)
	}
	if !e {
		t.Errorf("Expected OrderDishExists to return true, but got false.")
	}
}

func testOrderDishesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OrderDish{}
	if err = randomize.Struct(seed, o, orderDishDBTypes, true, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	orderDishFound, err := FindOrderDish(ctx, tx, o.OrderID, o.DishID)
	if err != nil {
		t.Error(err)
	}

	if orderDishFound == nil {
		t.Error("want a record, got nil")
	}
}

func testOrderDishesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OrderDish{}
	if err = randomize.Struct(seed, o, orderDishDBTypes, true, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = OrderDishes().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testOrderDishesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OrderDish{}
	if err = randomize.Struct(seed, o, orderDishDBTypes, true, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := OrderDishes().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testOrderDishesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	orderDishOne := &OrderDish{}
	orderDishTwo := &OrderDish{}
	if err = randomize.Struct(seed, orderDishOne, orderDishDBTypes, false, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}
	if err = randomize.Struct(seed, orderDishTwo, orderDishDBTypes, false, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = orderDishOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = orderDishTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := OrderDishes().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testOrderDishesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	orderDishOne := &OrderDish{}
	orderDishTwo := &OrderDish{}
	if err = randomize.Struct(seed, orderDishOne, orderDishDBTypes, false, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}
	if err = randomize.Struct(seed, orderDishTwo, orderDishDBTypes, false, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = orderDishOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = orderDishTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := OrderDishes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func orderDishBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *OrderDish) error {
	*o = OrderDish{}
	return nil
}

func orderDishAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *OrderDish) error {
	*o = OrderDish{}
	return nil
}

func orderDishAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *OrderDish) error {
	*o = OrderDish{}
	return nil
}

func orderDishBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *OrderDish) error {
	*o = OrderDish{}
	return nil
}

func orderDishAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *OrderDish) error {
	*o = OrderDish{}
	return nil
}

func orderDishBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *OrderDish) error {
	*o = OrderDish{}
	return nil
}

func orderDishAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *OrderDish) error {
	*o = OrderDish{}
	return nil
}

func orderDishBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *OrderDish) error {
	*o = OrderDish{}
	return nil
}

func orderDishAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *OrderDish) error {
	*o = OrderDish{}
	return nil
}

func testOrderDishesHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &OrderDish{}
	o := &OrderDish{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, orderDishDBTypes, false); err != nil {
		t.Errorf("Unable to randomize OrderDish object: %s", err)
	}

	AddOrderDishHook(boil.BeforeInsertHook, orderDishBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	orderDishBeforeInsertHooks = []OrderDishHook{}

	AddOrderDishHook(boil.AfterInsertHook, orderDishAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	orderDishAfterInsertHooks = []OrderDishHook{}

	AddOrderDishHook(boil.AfterSelectHook, orderDishAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	orderDishAfterSelectHooks = []OrderDishHook{}

	AddOrderDishHook(boil.BeforeUpdateHook, orderDishBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	orderDishBeforeUpdateHooks = []OrderDishHook{}

	AddOrderDishHook(boil.AfterUpdateHook, orderDishAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	orderDishAfterUpdateHooks = []OrderDishHook{}

	AddOrderDishHook(boil.BeforeDeleteHook, orderDishBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	orderDishBeforeDeleteHooks = []OrderDishHook{}

	AddOrderDishHook(boil.AfterDeleteHook, orderDishAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	orderDishAfterDeleteHooks = []OrderDishHook{}

	AddOrderDishHook(boil.BeforeUpsertHook, orderDishBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	orderDishBeforeUpsertHooks = []OrderDishHook{}

	AddOrderDishHook(boil.AfterUpsertHook, orderDishAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	orderDishAfterUpsertHooks = []OrderDishHook{}
}

func testOrderDishesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OrderDish{}
	if err = randomize.Struct(seed, o, orderDishDBTypes, true, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := OrderDishes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testOrderDishesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OrderDish{}
	if err = randomize.Struct(seed, o, orderDishDBTypes, true); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(strmangle.SetMerge(orderDishPrimaryKeyColumns, orderDishColumnsWithoutDefault)...)); err != nil {
		t.Error(err)
	}

	count, err := OrderDishes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testOrderDishToOneDishUsingDish(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local OrderDish
	var foreign Dish

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, orderDishDBTypes, false, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, dishDBTypes, false, dishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Dish struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.DishID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Dish().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	ranAfterSelectHook := false
	AddDishHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *Dish) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := OrderDishSlice{&local}
	if err = local.L.LoadDish(ctx, tx, false, (*[]*OrderDish)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Dish == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Dish = nil
	if err = local.L.LoadDish(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Dish == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testOrderDishToOneOrderUsingOrder(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local OrderDish
	var foreign Order

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, orderDishDBTypes, false, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, orderDBTypes, false, orderColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Order struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.OrderID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Order().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	ranAfterSelectHook := false
	AddOrderHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *Order) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := OrderDishSlice{&local}
	if err = local.L.LoadOrder(ctx, tx, false, (*[]*OrderDish)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Order == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Order = nil
	if err = local.L.LoadOrder(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Order == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testOrderDishToOneSetOpDishUsingDish(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a OrderDish
	var b, c Dish

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, orderDishDBTypes, false, strmangle.SetComplement(orderDishPrimaryKeyColumns, orderDishColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, dishDBTypes, false, strmangle.SetComplement(dishPrimaryKeyColumns, dishColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, dishDBTypes, false, strmangle.SetComplement(dishPrimaryKeyColumns, dishColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Dish{&b, &c} {
		err = a.SetDish(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Dish != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.OrderDishes[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.DishID != x.ID {
			t.Error("foreign key was wrong value", a.DishID)
		}

		if exists, err := OrderDishExists(ctx, tx, a.OrderID, a.DishID); err != nil {
			t.Fatal(err)
		} else if !exists {
			t.Error("want 'a' to exist")
		}

	}
}
func testOrderDishToOneSetOpOrderUsingOrder(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a OrderDish
	var b, c Order

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, orderDishDBTypes, false, strmangle.SetComplement(orderDishPrimaryKeyColumns, orderDishColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, orderDBTypes, false, strmangle.SetComplement(orderPrimaryKeyColumns, orderColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, orderDBTypes, false, strmangle.SetComplement(orderPrimaryKeyColumns, orderColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Order{&b, &c} {
		err = a.SetOrder(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Order != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.OrderDishes[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.OrderID != x.ID {
			t.Error("foreign key was wrong value", a.OrderID)
		}

		if exists, err := OrderDishExists(ctx, tx, a.OrderID, a.DishID); err != nil {
			t.Fatal(err)
		} else if !exists {
			t.Error("want 'a' to exist")
		}

	}
}

func testOrderDishesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OrderDish{}
	if err = randomize.Struct(seed, o, orderDishDBTypes, true, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testOrderDishesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OrderDish{}
	if err = randomize.Struct(seed, o, orderDishDBTypes, true, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := OrderDishSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testOrderDishesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &OrderDish{}
	if err = randomize.Struct(seed, o, orderDishDBTypes, true, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := OrderDishes().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	orderDishDBTypes = map[string]string{`OrderID`: `bigint`, `DishID`: `bigint`, `Quantity`: `integer`}
	_                = bytes.MinRead
)

func testOrderDishesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(orderDishPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(orderDishAllColumns) == len(orderDishPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &OrderDish{}
	if err = randomize.Struct(seed, o, orderDishDBTypes, true, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := OrderDishes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, orderDishDBTypes, true, orderDishPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testOrderDishesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(orderDishAllColumns) == len(orderDishPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &OrderDish{}
	if err = randomize.Struct(seed, o, orderDishDBTypes, true, orderDishColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := OrderDishes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, orderDishDBTypes, true, orderDishPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(orderDishAllColumns, orderDishPrimaryKeyColumns) {
		fields = orderDishAllColumns
	} else {
		fields = strmangle.SetComplement(
			orderDishAllColumns,
			orderDishPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := OrderDishSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testOrderDishesUpsert(t *testing.T) {
	t.Parallel()

	if len(orderDishAllColumns) == len(orderDishPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := OrderDish{}
	if err = randomize.Struct(seed, &o, orderDishDBTypes, true); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert OrderDish: %s", err)
	}

	count, err := OrderDishes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, orderDishDBTypes, false, orderDishPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize OrderDish struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert OrderDish: %s", err)
	}

	count, err = OrderDishes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
